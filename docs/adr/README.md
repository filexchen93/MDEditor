# Architecture Decision Records

ADRs record decisions that are expensive or risky to reverse. Use the next four-digit sequence, copy `template.md`, and open the decision for review before depending on it broadly.

Statuses are `Proposed`, `Accepted`, `Superseded` or `Deprecated`. Never rewrite the decision history to hide a change; add a superseding ADR and link both records.
