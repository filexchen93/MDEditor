{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Base capability for the main MDEditor window; no filesystem or network access.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
